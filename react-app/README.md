# Rights Quest India React Version

This folder contains a clean React + Firebase structure for the production version.

The working offline prototype remains in the project root as `index.html`, `styles.css`, and `script.js`.

To run this React version after installing Node.js:

```bash
npm install
npm run dev
```

Then add your Firebase credentials in `src/firebase.js`.
